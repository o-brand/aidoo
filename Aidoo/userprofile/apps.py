from django.apps import AppConfig


class UserprofileConfig(AppConfig):
    """This is the config class for the UserProfile app."""
    default_auto_field = "django.db.models.BigAutoField"
    name = "userprofile"
